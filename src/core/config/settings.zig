//! STARGUARD Configuration Settings
pub const Settings = struct {
    pub const default_quantum_threshold: f64 = 0.75;
    pub const max_pattern_cache_size: usize = 1024;
    pub const glimmer_enhancement_level: f32 = 0.85;
};
